# わからない時代から、決めれば進める時代へ

## AI時代の開発プロセスについて

ソフトウェア開発では、長いあいだ「ウォーターフォールか、アジャイルか」という問いが繰り返されてきた。

ウォーターフォールは、最初に要求を定義し、設計し、実装し、テストし、最後にリリースする。最初に全体を決めることができるなら、合理的な方法である。

しかし現実には、最初からすべてを知ることはできない。顧客が本当に欲しいものがわからない。技術的に何が可能なのかわからない。作ってみなければ使い勝手がわからない。

そこで登場したのがアジャイルだった。

小さく作る。試す。フィードバックを得る。また作る。

「最初に全部決められない」という現実を前提に、変化そのものを開発プロセスへ組み込んだ。アジャイルは、未知を前提とした開発方法なのである。

## わからない人間のためのアジャイル

アジャイルは「人間が何も考えなくてよい方法」ではない。

むしろ逆である。

何を作るのか。何を試すのか。どこまで作るのか。何をもって成功とするのか。

人間は何度も判断しなければならない。なぜなら、開発者自身がすべてを知っているわけではないからだ。

だからアジャイルでは、開発を小さく分割する。大きな問題を、小さな問題にする。そして、小さな問題を解きながら、次の問題を発見する。

## しかしAIは「わからない」の意味を変える

AIに同じ仕事をさせると、少し事情が違ってくる。

たとえば、「VueでGitHub Pagesに公開するライブ検索アプリを作る」という目的があり、「データはJSON」「週一回クロールする」「GitHub Actionsでテストする」「GitHub Pagesへデプロイする」と決まっているなら、AIにとって、それは一つずつ人間に確認しながら進める仕事ではない。

設計が十分に宣言されているなら、設計からコードを生成し、コードからテストを作り、テストを実行し、デプロイまで進められる。

ここでは、開発のボトルネックは「作業」ではない。「決定」になる。

> AI時代には、実装能力よりも未決定事項のほうが開発を止める。

## テーゼ：ウォーターフォールへ戻る

ここで、一見するとウォーターフォールが復活する。

最初に宣言する。目的。制約。技術。データ形式。公開先。成功条件。

それが決まっていれば、AIはかなり先まで一気に進める。

人間がコードを一行ずつ書く必要がないなら、開発プロセスは再び「上流から下流へ流れる」形を取り戻す。

```text
Goal
 ↓
Design
 ↓
Code
 ↓
Test
 ↓
Deploy
```

これはウォーターフォールに似ている。

しかし、昔のウォーターフォールとは決定的に違う。昔は、人間が各工程を作業していた。AI時代には、人間は工程を宣言し、AIが工程を実行する。

## アンチテーゼ：それでも全部は決められない

しかし、「最初に全部決めればよい」とはならない。

現実には未決定事項が残る。どのデータソースを使うのか。どのUIがよいのか。どの仕様を採用するのか。どの品質を合格とするのか。

さらに、AIが実行して初めて発見される問題もある。

つまり、「最初に全部決める」というウォーターフォールの前提は、AI時代になっても完全には成立しない。

ここでアジャイルが再び意味を持つ。未知があるなら、質問する。試す。結果を見る。判断する。

ただし、AIによって役割が変わる。

## 人間は「作業者」から「決定者」へ

AI時代のアジャイルでは、人間がすべてを細かく指示する必要はない。

AIは、既知の部分を自分で進めればよい。そして、自分では決めてはいけない場所だけ止まる。

```yaml
decision:
  id: deployment
  status: undecided
  question: "公開先を決めてください"
  options:
    - github-pages
    - vercel
    - cloudflare-pages
```

AIはここで勝手に選ばない。「どれにしますか？」と聞く。人間が答える。するとAIは、その決定を設計へ反映し、また先へ進む。

人間はコードを書く必要がない。しかし、人間の判断は消えていない。むしろ、判断だけが純化されている。

## 止揚：宣言的ウォーターフォール＋局所的アジャイル

ここで、ウォーターフォールとアジャイルは対立しなくなる。

ウォーターフォールは「決まっているものを一気に進める」。アジャイルは「決まっていないものを小さく決めていく」。

この二つを合わせる。

```text
宣言
 ↓
AIが一気に展開
 ↓
未決定を検出
 ↓
Demand
 ↓
人間＋AIで決定
 ↓
宣言を更新
 ↓
AIが再開
```

つまり、**全体はウォーターフォール的に、未決定部分だけアジャイル的に進める。**

これがAI時代の開発プロセスになる。

私はこれを「宣言的開発」と呼びたい。

## 開発とは、作ることではなく「未決定を減らすこと」になる

このモデルでは、開発の進捗を見る指標も変わる。

従来なら「何％実装したか」を見る。しかしAI時代には、「何％決定したか」も重要になる。

```text
Progress       72%
Decisions       8
Undecided       2
Blocked         1
Bottlenecks     1
```

このときAIに必要なのは、さらにコードを書くことではない。未決定の2つを発見し、「ここを決めてください」と人間へ返すことである。

だからAIエージェントは、単なる自動実装装置ではなく、**未決定事項を発見する装置**になる。

## Graphは進捗表ではなく、設計そのものになる

この考え方では、開発グラフも単なるタスク管理表ではない。

NodeはGoal、Decision、Phase、Task、Gate、Automation、Demandを表す。

Edgeは単なる線ではなく、demand、answer、result、pass、block、depends_onという「やりとり」を表す。

```text
Goal
 ↓ demand
Decision
 ↓ answer
Phase
 ↓ demand
Task
 ↓ result
Gate
 ↓ pass
Next Phase
```

設計は、人間が一度完成させる文書ではない。**Block同士のやりとりによって完成していく状態**になる。

## AIに任せることと、人間が決めること

AIには、既知の設計を展開する、コードを書く、テストする、結果を確認する、ボトルネックを検出する、未決定事項を質問する、決定をYAMLへ反映する、といった仕事を任せる。

一方、人間にはGoal、制約、意思決定、Gate、方向転換を残す。

つまり、

> **AIは勝手に決めない。しかし、決まったことは遠慮なく進める。**

この境界が重要になる。

## おわりに――開発の中心は「コード」から「宣言」へ

AIによって、コードを書くことの価値がなくなるわけではない。しかし、コードを書くことが開発の中心ではなくなる可能性がある。

中心になるのは、何を実現するのか。何が決まっているのか。何が決まっていないのか。誰が決めるのか。そして、決まった瞬間に、どこまでAIが進められるのか、という問題である。

アジャイルは「わからないこと」を扱うために生まれた。ウォーターフォールは「決まったこと」を順序立てて実行するために強かった。AIは、「決まったこと」を極めて速く展開できる。

だから、両者を対立させる必要はない。

**決まっているところはウォーターフォールのように進める。決まっていないところだけアジャイルのように対話する。**

その境界をGraphとYAMLで明示する。そしてAIは、その境界に到達したときだけ人間へDemandを返す。

これからの開発プロセスは、

> **「人間が一つずつ作るプロセス」から、「人間が決め、AIが進み、未決定だけを共同で決めるプロセス」へ。**

変わっていくのではないだろうか。
